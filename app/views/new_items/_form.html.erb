<%= form_for(@new_item) do |f| %>
  <% if @new_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_item.errors.count, "error") %> prohibited this new_item from being saved:</h2>

      <ul>
      <% @new_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :itemtype %><br>
    <%= f.text_field :itemtype %>
  </div>
  <div class="field">
    <%= f.label :itemid %><br>
    <%= f.text_field :itemid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
